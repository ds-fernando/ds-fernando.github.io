{
  "name": "webpersonal",
  "version": "1.0.0",
  "main": ".eleventy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx @11ty/eleventy --serve",
    "clean": "rm -rf docs",
    "build": "eleventy",
    "deploy": "npm run build && git add . && git commit -m '🚀 Deploy actualizado' && git push origin main",
    "deploy-subdomain": ""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
